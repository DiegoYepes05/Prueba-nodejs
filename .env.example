# Conexión a PostgreSQL. Ajusta usuario/clave/puerto según tu entorno.
# Coincide con el docker-compose.yml incluido en el repo.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/confiabilidad?schema=public"

# Puerto en el que escucha la API
PORT=3000
